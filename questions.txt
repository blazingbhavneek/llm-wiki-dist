All:

直接編成ファイルにおいて、転送効率を最適化するためのカテゴリおよびサブファイルの設計基準（推奨されるファイル数やデータサイズの閾値）は何か。
 
非同期N重書きで利用される「差分管理情報」の具体的なデータ構造と、大規模ファイルコピーにおける実装との共通点は何か。
 
従系CPUへのN重書き失敗が構成制御に通知された後、システムはどのような状態遷移（抑止、再同期、切り離しなど）を行うのか。
 
圧縮ファイルにおいて、主系・従系間で圧縮回数が不整合になった場合の検知および回復手段は定義されているか。
 
ファイルレベルのロック（S, F）とカテゴリレベルのロック（C, FC）が混在する場合の優先順位および排他制御の挙動はどうなるか。
 
保存用圧縮属性を持つファイルを「ファイルコピー」した場合、圧縮された状態のままコピーされるのか、あるいは内部的に伸長してコピーされるのか。
 
大規模ファイルコピーにおいて、リモートコピー時の「on/subリージョン同一性」の制約はどのように適用されるか、あるいはこの制約をバイパスするのか。
 
N重書きにおいて、従系への書き込み失敗により主系と不整合が発生した際、どのようにして従系を最新状態に同期（回復）させるのか。
 
サブファイルやカテゴリを利用してファイルをまとめた際、具体的にどのようなメカニズムでTCP/IP上の転送回数が削減され、高速化されるのか。
 
大規模ファイルコピーにおいて、なぜ「カテゴリロック」が指定できず、「サブファイルロック」が必須となるのか。
 
「N重書き圧縮ファイル」としての永続的な圧縮状態と、「リモートライト時の一時的な圧縮転送」は、内部的に同じ圧縮方式（ZLIB等）を使用しているのか、あるいは異なる管理単位なのか。
 
非同期N重書きにおける差分管理方法が「大規模ファイルコピーと同じ」とされるが、具体的にどのようなアルゴリズム（チェックサム、ブロック管理、タイムスタンプ等）で差分を検知し転送しているのか。
 
圧縮ファイルにおいて、差分コピーの判定単位となる「サブファイル」の定義は何か。また、属性定義の「on/sub属性を持てない」という制約とどう整合するのか。
 
圧縮ファイルに対する「N重書き（定常的なデータ同期）」と「mpf_mfs_copycompressfile（引継コピー）」は、どのようなタイミングで使い分けられ、互いにどう影響し合うのか。
 
従系CPUへのN重書き失敗後、どのような条件（回数閾値など）で「N重書き抑止」や「重故障処理」へ遷移し、その際のデータ不整合をどのように解消するのか。
 
非同期N重書きにおける「定周期」のデフォルト値および設定可能範囲は何か。また、更新頻度が高いファイルにおける周期設定とデータ損失リスク（RPO）のトレードオフはどうなっているか。
 
MFSファイルコピーにおいて、サブファイル数が多い場合に性能劣化が顕著になる具体的な境界値（閾値）はいくつか。
 
圧縮ファイルコピー (`mpf_mfs_copycompressfile`) の実行中にアプリケーションによるファイルアクセスが割り込んだ場合、コピーの一貫性はどのように担保されるか。
 
定義ファイルにおける値域（カテゴリ数 0-127、ファイル番号 0-8192等）を超過して設定した場合のシステム挙動はどのようなものか。
 
セーブ機能とブロードキャスト機能を併用した際に、応答性が保証できなくなる具体的な条件（閾値）は何か。
 


Selected:

ファイルレベルのロック（S, F）とカテゴリレベルのロック（C, FC）が混在する場合の優先順位および排他制御の挙動はどうなるか。

N重書きにおいて、従系への書き込み失敗により主系と不整合が発生した際、どのようにして従系を最新状態に同期（回復）させるのか。

MFSファイルコピーにおいて、サブファイル数が多い場合に性能劣化が顕著になる具体的な境界値（閾値）はいくつか。

定義ファイルにおける値域（カテゴリ数 0-127、ファイル番号 0-8192等）を超過して設定した場合のシステム挙動はどのようなものか。
