prompt

the current linking logic is absolute garbage its super long af, while its running my users would rather read the whole book and become master in it. 

so we have to plant some seeds at wiki generation time: so in all docs containing any information, what kind of info it has? 

Entities, behaviour (how entities are behaving, what is happening here), a running context that contains the above two, what else we are missing?

My idea is, when we are making wikis, i think we a few extra calls, we can do some stuff, for example in our observation pass we can try to make a list of what kind of entites this has, people, company, api, etc etc that are being repeated everywhere
these are the "tags/entities" which is first unit of tracking

once we know what dominant ones are, in the next seed rewriting pass, we can track in each page which entity is defined and used, so during intra page and inter page linking phase, we can easily programatically make links where entity used in this wiki is defined first, no? do you see this? we can easily make a inline link here for when an 
entity is used in the page, pointing to a page where it was defined (no need to do this everytime, just add an inline link at the first mention of that entity, no need to do it every time its mentioned in the page)

then there is "behaviours" in each page, what are the "entities" "doing" in this page, this is kidn of indepedant of names of the entites, its like whats happeningin here, and this the one that links the entites to each other, the entities exxtracted from the page interact with each other 
through these "behaviours" , we can use this list of behaviour edges, to kind of maitain a multihop graph, since documents might have cross documeent entity mentions, we can make a graph with those

if page 1 mentions A and B interacting with behaviour C1, and page 2 mentions B and D using behaviour C2, then page A and D are linked by B1 + B2

so rather than making a "DEEP reseracher" in linking phase who has to read through all documents cross documents etc, we can show them 1-3 hops graph of all entity mentioned in page + their interactions, now entity definition usage graphs are programmatic, in the wiki rewrite this is like the minimal change to all the page involved, very very quick llm calls
but whenever a new page shows up, rather than us exploring every page involved, we can make a entity -> behaviour subgraph of a page, filter out the pages which will already come up during query time due to embedding similarity + keyword match (since RAG already does that), filter out the most sensible ones using LLMs (ask it 4-5 together at a time so it doesnt get lost in infiinite reasoning), 
then have it make the links and read pages and write those links, basics one which is entity usage + define pages are v v quick, we can do embedding simiarity + reranker on behaviour list of each page + the keyword matches/similarity and show user similar things are discussed here, refer "this" page for this

but multi hop researchers can be  done programatically. Also for query time, maybe we can make this 1-3 hop entity behaviour links at footers programatticaly so an LLM looking at a page can go to the page which is relevant to query of that time.


